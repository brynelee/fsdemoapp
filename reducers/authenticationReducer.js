import {
    LOGIN,
    LOGOUT
} from "../actions/types";

const reducer = (state = [], action) => {
    console.log("authentication reducer was called ...");

    switch(action.type) {
        case LOGIN:
            return 
    }
    
}

export default reducer;

